name: Action Test
on: 
  workflow_dispatch:
  push:
    paths:
      - '**.yml'
  pull_request:
    paths:
      - '**.yml'

defaults:
  run:
    shell: bash

jobs:
  test1:
    uses: ./.github/workflows/env.yml
    with:
      content: "2.0.0"
      key: "version"
      files: |
        ./tests/test1.json
        ./tests/test1.toml

  test2:
    uses: ./.github/workflows/env.yml
    with:
      content: "very cool"
      key: "random"
      files: |
        ./tests/test2.json
        ./tests/test2.toml
          
      
      
